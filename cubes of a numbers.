#include<stdio.h>    
int main()    
{    
 int n, cube;
 printf("please enter any integer value:");
 scanf("%d", &n);
 cube = n*n*n;
 printf("cube of a given number is %d is = %d", n, cube);
 return 0;
 }
